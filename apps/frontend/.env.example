
# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Host to bind the server to (use 0.0.0.0 for Docker/production)
HOST=0.0.0.0

# Public URL of the application (optional - auto-detected if not set)
# For production: https://api.yourdomain.com
# For local: leave empty to auto-detect as http://localhost:4200
APP_URL=
